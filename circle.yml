machine:
  java:
    version: openjdk8

dependencies:
  override:
    - ./gradlew build
  cache_directories:
      - "~/.m2/"
      - "~/.ivy2/"
      - "~/.sbt/"
      - "~/.gradle/"

test:
  override:
    - ./gradlew jacocoTestReport coveralls
