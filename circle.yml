machine:
  java:
    version: openjdk8

dependencies:
  override:
      - ./gradlew clean war
  cache_directories:
      - "~/.m2/"
      - "~/.ivy2/"
      - "~/.sbt/"
      - "~/.gradle/"

test:
  override:
      - ./gradlew jacocoTestReport coveralls
